#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
ll MOD = 1000000007;

int main()
{
    int N;
    cin >> N;
    vector <int> a(N, 0);
    for (int i = 0; i < N; i++) cin >> a[i];
    
    return 0;
}
